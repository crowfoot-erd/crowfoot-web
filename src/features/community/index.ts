/**
 * 커뮤니티 feature 공개 면 — 훅(쿼리·뮤테이션)과 코멘트 섹션.
 * MarkdownEditor/MarkdownViewer는 lazy 대상이라 default export 그대로 경로 임포트한다.
 */
export * from '@/features/community/hooks'
export { CommentSection } from '@/features/community/components/comment-section'
