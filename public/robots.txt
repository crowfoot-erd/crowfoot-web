# 검색엔진 크롤링 안내 — 색인 대상은 public/sitemap.xml과 같은 공개 페이지뿐이다.
# /share/{token}은 토큰이 곧 자격이라 크롤링 자체를 차단한다(토큰 유출 방지).
# /login·/auth/callback은 크롤을 허용하되 페이지 메타(noindex)로 색인을 막는다.
User-agent: *
Disallow: /share/

Sitemap: https://crowfoot.java21.net/sitemap.xml
